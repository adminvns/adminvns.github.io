{
  "name": "resume_tester",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "npx cypress run --record --key ${{ secrets.CYPRESS_RECORD_KEY }} --browser chrome",
    "testlocal": "npx cypress run  --browser chrome --headed"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.6.4",
    "cypress-xpath": "^1.8.0"
  },
  "devDependencies": {
    "cypress": "^13.6.2"
  }
}
